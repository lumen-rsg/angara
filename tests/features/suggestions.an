attach io;

// Test that the compiler produces "did you mean?" suggestions
// for misspelled identifiers and types.

export func main() -> i64 {
    io.println(1, "=== Suggestion Tests ===");

    // This file just needs to compile and run.
    // Suggestion testing is done by checking compiler output.

    io.println(1, "=== Suggestion Tests Passed ===");
    return 0;
}
