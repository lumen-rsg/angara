// M21: negative tests for async/await (await outside async,
// async foreign, async intrinsic).
// These features had zero negative test coverage.
// expect: E417

// E417: async function cannot be foreign (parse error — fires first)
async foreign func bad_foreign() -> i64;

// E418: async function cannot be intrinsic
// async intrinsic func bad_intrinsic() -> i64;

export func main() -> i64 {
    return 0;
}
