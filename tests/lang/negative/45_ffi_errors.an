// M21: negative tests for FFI (foreign export, pointer dereference).
// Note: parse errors abort before type-checking, so E271 and E379
// cannot coexist in the same file. E271 is tested here; E379 is in 53_*.
// expect: E271

// E271: a foreign function cannot be exported
export foreign func bad_export() -> i64;

export func main() -> i64 {
    return 0;
}
