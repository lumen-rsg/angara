// M21: negative test for async intrinsic function.
// The parser requires 'func' immediately after 'async', so E416 fires
// before the semantic check for async+intrinsic (E418).
// expect: E416

// E416: Expected 'func' after 'async'
async intrinsic func bad_intrinsic() -> i64;

export func main() -> i64 {
    return 0;
}
